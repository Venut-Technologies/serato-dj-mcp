# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# Claude Code / Superpowers — kept local, never committed
.claude/
.superpowers/
docs/superpowers/
CLAUDE.md

# Local Serato fixtures (contains Serato-owned demo content — do not redistribute)
.local-fixtures/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local env
.env
.env.*
!.env.example

# npm
*.tgz
npm-debug.log*

# A Serato library's own files, in case one is ever staged by accident.
# The test fixtures are .sql, never these.
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.crate
database V2

# Agent instructions live in the private governance repository (symlinked, never committed)
AGENTS.md
CLAUDE.local.md
